spring:
  datasource:
    url: jdbc:postgresql://otus_citus_master:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  jackson:
    date-format: yyyy-MM-dd
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: mySuperSecretKeyForJWTTokenGenerationThatIsAtLeast32BytesLong2024!
  expiration: 86400000 # 24 hours in milliseconds

server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always